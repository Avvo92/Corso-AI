# Ripasso frontend — React / Node.js (pre-M10)

> **Stato:** 📌 **Parcheggiato** (canonizzato 25/08/2026).  
> **Non iniziare** mentre il focus è M3–M9 (AI).  
> **Attivare:** fine Modulo 9 oppure Fase 0b / inizio Modulo 10, **prima** di costruire le UI React di produzione.

## Perché esiste

Richiesta dello studente (Gianluca): esperienza pregressa su **React** e **Node.js**, ma **arrugginita**. Quando si costruiscono Validator e Replicator in stack **React + FastAPI** (M10), il lavoro va impostato in modo **didattico** (ripasso + ponte prodotto), non “buttare UI a caso”.

Fonte di verità corso: `CONTESTO_CORSO.md` → Profilo → **Impegno canonico — Ripasso frontend React / Node**.

## Allineamento prodotto

| Fase corso | UI prevista |
|------------|-------------|
| M4–M7 | Prototipi **Streamlit** (veloci) |
| M10 | Produzione **React + FastAPI** |

Vedi: `docs/prodotto/APPUNTI_APPLICATIVO_REPLICATOR.md` §7.2, `roadmap_ai.md` Modulo 10 Fase **0b**.

## Cosa produrre (quando si attiva)

Mini-percorso nello **stesso stile del corso AI**:

1. analogia semplice  
2. pezzo piccolo di codice  
3. mini-esercizio  
4. ponte esplicito a Validator / Replicator  

Cartella prevista (da creare all’attivazione): file capitolo tipo `01_node_npm.md`, `02_react_componenti.md`, … oppure `.tsx` guidati — decisione del mentor al momento dell’apertura.

### Scope minimo suggerito

1. Node / npm / script / env  
2. React: componenti, props, state, effetti  
3. `fetch` verso FastAPI  
4. Form + lista esiti (pezzo Validator)  
5. Upload + anteprima PDF (pezzo Replicator)  
6. Opzionale: TypeScript leggero, router  

**Non** è un corso React completo da zero: è un **brush-up mirato**.

## Vincolo mentor

- Non deragliare i moduli AI per fare React in anticipo.  
- All’apertura M10 Fase Frontend: **verificare** che questo ripasso sia stato fatto o avviarlo subito.
